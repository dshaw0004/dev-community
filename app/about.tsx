import {View} from 'react-native';
import {Text} from 'react-native-paper';

export default function About(){
        return (
                <View>
                <Text>
                this app does not own any of post data, it is just a wrapper around the dev.to api
                </Text>
                </View>
        );
}
